<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AllinCompose"
        tools:targetApi="31">
        <activity
            android:name=".screen.placeholder.PlaceholderActivity"
            android:exported="false"
            android:label="@string/title_activity_placeholder"
            android:theme="@style/Theme.AllinCompose" />
        <activity
            android:name=".view.creditcard.CreditCardScreen"
            android:exported="false"
            android:label="@string/title_activity_credit_card_screen"
            android:theme="@style/Theme.AllinCompose" />
        <activity
            android:name=".screen.creditcard.CreditCardActivity"
            android:exported="false"
            android:label="@string/title_activity_credit_card"
            android:theme="@style/Theme.AllinCompose" />
        <activity
            android:name=".screen.main.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AllinCompose">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>